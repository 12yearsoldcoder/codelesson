import math
print(int(math.log(16,2)))
# 로그란 밑과 지수로 log2(16) 중 2는 및, 16은 지수로 16이 2의 몇 제곱인지 구하는 식이다. 로그연산이라고 부른다. 정렬을 할땐 O(n log n)이라고 한다.
# O(n^2) 정렬은 매우 느린 정렬로 O는 개수라고 보면 된다. 속도는 n^2로 O^2와 같다. O가 10이면 100초가 걸린다.
